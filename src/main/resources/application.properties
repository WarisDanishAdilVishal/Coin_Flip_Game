# Database Configuration
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
#spring.datasource.url=jdbc:postgresql://localhost:5432/coingame
# spring.datasource.username=postgres
# spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
#spring.jpa.hibernate.ddl-auto=validate
spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.defer-datasource-initialization=true

# SQL Configuration
# spring.sql.init.mode=always
# spring.sql.init.continue-on-error=true
# spring.sql.init.platform=postgresql
# spring.sql.init.schema-locations=classpath:schema.sql

# Server Configuration
#server.port=8080

# JWT Configuration
app.jwt.secret=QkQ2NEVGRjAtQzBBNC00MzExLUFFMTMtNkMzNTBDMDU1MzIwMTIzNDU2Nzg5MEFCQ0RFRkdISUpLTE1OT1BRUlNUVVZXWFla
app.jwt.expiration=30000000000
#app.jwt.refresh-expiration=600000

# CORS Configuration
 app.cors.allowed-origins=${FRONTEND_URL}
#app.cors.allowed-origins=http://localhost:4200

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
#spring.mail.username=danishadil781@gmail.com
spring.mail.username=${MAIL_USERNAME}
#spring.mail.password=gxct cnrd kbwn bpgz
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Reset Password Configuration
app.reset-password.token-expiration=3600000
app.reset-password.url=${FRONTEND_URL}/reset-password
#app.reset-password.url=http://localhost:4200/reset-password

# Logging Configuration
logging.level.org.springframework.security=DEBUG
# logging.level.org.springframework.web=DEBUG
# logging.level.com.casino.coinflip=DEBUG
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql=TRACE